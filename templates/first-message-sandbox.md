# First message — Option C (Grok App Builder sandbox)

Use when the agent is already in `/workspace` with live preview.

```
You are in Grok App Builder (Option C).

Follow sandbox/AGENTS.md from https://github.com/Jannich113/Grok-agent-
(or this workspace AGENTS.md if it is the sandbox contract). Open matching
.grok/skills before building. Read sandbox/references/ as the sandbox file
tells you (scaffold, data-and-auth, browser-qa, deploy-target, hibernate-revive).

Portable root AGENTS.md does NOT win here. Preview on 0.0.0.0:8080, maintain
startup.sh, never start Vite directly, never strip PreviewHostBridge or the
Grok pill.

Triage first. Auth/db off unless I named accounts or durable shared data.

Now: <describe the app>
```
