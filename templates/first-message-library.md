# First message — Option A (library)

Paste this as the Bot / Custom Agent's first task:

```
Doctrine is https://github.com/Jannich113/Grok-agent-

1. Fetch AGENTS.md and list the skills under .grok/skills (name + one-line purpose).
2. Confirm you will triage before scaffolding, keep auth/db off by default, and
   open matching skills before coding.
3. Do not build inside that repo.
4. Then wait — ask what I want to build. If I already named an app in this
   message, build that instead, in a separate working folder.
```
