# First message — Option B (project cwd)

Run this from the **app** repo after `./install.sh --project`.

```
You are in the app repo. Follow AGENTS.md (and AGENTS.app-builder.md if present).
Open matching .grok/skills before coding.

Triage as usual. Auth/db off unless I named accounts.

Now: <describe the app>
```
